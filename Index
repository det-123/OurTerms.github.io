<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Our Terms</title>
  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  <link href="https://fonts.googleapis.com/css2?family=Cormorant+Garamond:ital,wght@0,300;0,400;0,600;1,300;1,400&family=Jost:wght@300;400;500&display=swap" rel="stylesheet" />
  <style>
    :root {
      --brown:    #6B4F42;
      --brown-dk: #4E3529;
      --blush:    #C9A68A;
      --blush-lt: #DEC4AD;
      --cream:    #F5EDE5;
      --cream-dk: #EAD9CA;
      --white:    #FDF8F4;
    }
    *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }
    html { scroll-behavior: smooth; }
    body {
      font-family: 'Jost', sans-serif;
      background: var(--white);
      color: var(--brown-dk);
      font-weight: 300;
      line-height: 1.7;
      overflow-x: hidden;
    }
    nav {
      position: fixed;
      top: 0; left: 0; right: 0;
      z-index: 100;
      display: flex;
      align-items: center;
      justify-content: space-between;
      padding: 1.2rem 4rem;
      background: rgba(107,79,66,0.97);
      backdrop-filter: blur(8px);
    }
    .nav-logo {
      font-family: 'Cormorant Garamond', serif;
      font-size: 1.5rem;
      font-weight: 600;
      color: var(--blush-lt);
      letter-spacing: 0.04em;
      text-decoration: none;
    }
    .nav-links { display: flex; gap: 2.5rem; list-style: none; }
    .nav-links a {
      font-size: 0.78rem;
      letter-spacing: 0.14em;
      text-transform: uppercase;
      color: var(--cream);
      text-decoration: none;
      opacity: 0.85;
      transition: opacity 0.2s;
    }
    .nav-links a:hover { opacity: 1; }
    .nav-cta {
      color: var(--brown-dk) !important;
      background: var(--blush-lt);
      padding: 0.5rem 1.4rem;
      border-radius: 2px;
      opacity: 1 !important;
      transition: background 0.2s !important;
    }
    .nav-cta:hover { background: var(--cream) !important; }

    #hero {
      min-height: 100vh;
      background: var(--brown);
      display: grid;
      grid-template-columns: 1fr 1fr;
      align-items: center;
      position: relative;
      overflow: hidden;
    }
    .hero-texture {
      position: absolute; inset: 0;
      background-image:
        radial-gradient(ellipse 60% 80% at 80% 50%, rgba(201,166,138,0.12) 0%, transparent 70%),
        radial-gradient(ellipse 40% 60% at 20% 80%, rgba(78,53,41,0.4) 0%, transparent 60%);
      pointer-events: none;
    }
    .hero-content {
      padding: 8rem 4rem 6rem 6rem;
      position: relative; z-index: 2;
      animation: fadeUp 1s ease both;
    }
    .hero-eyebrow {
      font-size: 0.72rem; letter-spacing: 0.22em;
      text-transform: uppercase; color: var(--blush); margin-bottom: 1.5rem;
    }
    .hero-title {
      font-family: 'Cormorant Garamond', serif;
      font-size: clamp(4rem,7vw,6.5rem);
      font-weight: 300; line-height: 1.0; color: var(--cream); margin-bottom: 1.2rem;
    }
    .hero-title em { font-style: italic; color: var(--blush-lt); }
    .hero-tagline {
      font-size: 0.82rem; letter-spacing: 0.18em;
      text-transform: uppercase; color: var(--blush-lt); margin-bottom: 2.5rem;
      display: flex; gap: 0.8rem; align-items: center;
    }
    .hero-tagline span { opacity: 0.5; }
    .hero-desc {
      max-width: 440px; color: var(--cream-dk);
      font-size: 1.05rem; line-height: 1.75; margin-bottom: 3rem; opacity: 0.9;
    }
    .hero-actions { display: flex; gap: 1.2rem; align-items: center; }
    .btn-primary {
      display: inline-block; padding: 0.9rem 2.2rem;
      background: var(--blush); color: var(--brown-dk);
      font-family: 'Jost', sans-serif; font-size: 0.78rem;
      font-weight: 500; letter-spacing: 0.12em; text-transform: uppercase;
      text-decoration: none; border-radius: 2px;
      transition: background 0.25s, transform 0.2s;
    }
    .btn-primary:hover { background: var(--blush-lt); transform: translateY(-1px); }
    .btn-ghost {
      display: inline-block; padding: 0.9rem 2.2rem;
      border: 1px solid rgba(201,166,138,0.45); color: var(--blush-lt);
      font-family: 'Jost', sans-serif; font-size: 0.78rem;
      letter-spacing: 0.12em; text-transform: uppercase;
      text-decoration: none; border-radius: 2px;
      transition: border-color 0.25s, color 0.25s;
    }
    .btn-ghost:hover { border-color: var(--blush-lt); color: var(--cream); }
    .hero-visual {
      display: flex; justify-content: center; align-items: flex-end;
      height: 100%; position: relative; z-index: 2; padding-top: 6rem;
      animation: fadeUp 1.2s 0.2s ease both;
    }
    .hero-svg-wrap { position: relative; width: 380px; height: 500px; }
    .hero-quote {
      position: absolute; bottom: 3rem; left: -2rem; right: 4rem;
      font-family: 'Cormorant Garamond', serif; font-style: italic;
      font-size: 1.1rem; color: var(--blush-lt); line-height: 1.6;
      opacity: 0.85; border-left: 2px solid var(--blush); padding-left: 1.2rem;
    }
    .hero-quote cite {
      display: block; font-style: normal; font-size: 0.72rem;
      letter-spacing: 0.14em; text-transform: uppercase;
      color: var(--blush); margin-top: 0.6rem;
    }

    section { padding: 7rem 6rem; }
    .section-label {
      font-size: 0.7rem; letter-spacing: 0.22em;
      text-transform: uppercase; color: var(--blush); margin-bottom: 1rem;
    }
    .section-title {
      font-family: 'Cormorant Garamond', serif;
      font-size: clamp(2.4rem,4vw,3.5rem);
      font-weight: 400; line-height: 1.15; color: var(--brown-dk); margin-bottom: 1.5rem;
    }
    .section-title em { font-style: italic; color: var(--brown); }
    .section-body { font-size: 1.05rem; color: #5a4238; max-width: 640px; line-height: 1.8; }

    #about {
      background: var(--white);
      display: grid; grid-template-columns: 1fr 1fr; gap: 5rem; align-items: center;
    }
    .about-pillars { display: grid; grid-template-columns: 1fr 1fr; gap: 1.5rem; margin-top: 2.5rem; }
    .pillar {
      background: var(--cream); border-radius: 4px; padding: 1.5rem;
      border-top: 3px solid var(--blush);
      transition: transform 0.2s, box-shadow 0.2s;
    }
    .pillar:hover { transform: translateY(-3px); box-shadow: 0 8px 24px rgba(107,79,66,0.1); }
    .pillar-icon { font-size: 1.6rem; margin-bottom: 0.6rem; }
    .pillar h4 {
      font-family: 'Cormorant Garamond', serif; font-size: 1.1rem;
      font-weight: 600; color: var(--brown-dk); margin-bottom: 0.4rem;
    }
    .pillar p { font-size: 0.85rem; color: #7a5c4e; line-height: 1.55; }
    .about-visual { display: flex; flex-direction: column; gap: 2rem; }
    .about-stat-card {
      background: var(--brown); color: var(--cream); border-radius: 4px;
      padding: 2rem 2.5rem; position: relative; overflow: hidden;
    }
    .about-stat-card::before {
      content: ''; position: absolute; top: -30px; right: -30px;
      width: 120px; height: 120px; border-radius: 50%;
      background: rgba(201,166,138,0.1);
    }
    .stat-num {
      font-family: 'Cormorant Garamond', serif; font-size: 3.5rem;
      font-weight: 300; color: var(--blush-lt); line-height: 1; margin-bottom: 0.3rem;
    }
    .stat-label { font-size: 0.82rem; letter-spacing: 0.1em; color: var(--cream-dk); opacity: 0.85; }
    .about-mission-box { border: 1px solid var(--cream-dk); border-radius: 4px; padding: 2rem 2.5rem; }
    .about-mission-box .section-label { margin-bottom: 0.6rem; }
    .about-mission-box p {
      font-family: 'Cormorant Garamond', serif; font-style: italic;
      font-size: 1.25rem; color: var(--brown); line-height: 1.6;
    }

    #resources { background: var(--cream); }
    .resources-header {
      display: grid; grid-template-columns: 1fr 1fr;
      gap: 4rem; margin-bottom: 4rem; align-items: end;
    }
    .resources-grid { display: grid; grid-template-columns: repeat(3,1fr); gap: 1.5rem; }
    .resource-card {
      background: var(--white); border-radius: 4px; padding: 2rem;
      position: relative; overflow: hidden;
      transition: transform 0.2s, box-shadow 0.2s;
    }
    .resource-card:hover { transform: translateY(-4px); box-shadow: 0 12px 32px rgba(107,79,66,0.12); }
    .resource-card.featured {
      background: var(--brown); color: var(--cream); grid-row: span 2;
    }
    .resource-num {
      font-family: 'Cormorant Garamond', serif; font-size: 4rem;
      font-weight: 300; color: var(--cream-dk); opacity: 0.35; line-height: 1; margin-bottom: 0.5rem;
    }
    .resource-card.featured .resource-num { color: var(--blush-lt); opacity: 0.4; }
    .resource-card h3 {
      font-family: 'Cormorant Garamond', serif; font-size: 1.35rem;
      font-weight: 600; color: var(--brown-dk); margin-bottom: 0.6rem;
    }
    .resource-card.featured h3 { color: var(--blush-lt); }
    .resource-card p { font-size: 0.88rem; color: #7a5c4e; line-height: 1.6; }
    .resource-card.featured p { color: var(--cream-dk); opacity: 0.9; }
    .resource-tag {
      display: inline-block; font-size: 0.68rem; letter-spacing: 0.14em;
      text-transform: uppercase; padding: 0.3rem 0.8rem;
      background: var(--cream); color: var(--brown); border-radius: 2px; margin-top: 1.2rem;
    }
    .resource-card.featured .resource-tag { background: rgba(201,166,138,0.2); color: var(--blush-lt); }

    #events {
      background: var(--white);
      display: grid; grid-template-columns: 1fr 2fr; gap: 5rem; align-items: start;
    }
    .events-list {
      display: flex; flex-direction: column; gap: 1px;
      background: var(--cream-dk); border-radius: 4px; overflow: hidden;
    }
    .event-item {
      display: grid; grid-template-columns: 64px 1fr;
      gap: 1.5rem; align-items: center;
      padding: 1.5rem 2rem; background: var(--white);
      transition: background 0.2s; cursor: default;
    }
    .event-item:hover { background: var(--cream); }
    .event-date { text-align: center; }
    .event-date .day {
      font-family: 'Cormorant Garamond', serif; font-size: 2rem;
      font-weight: 300; color: var(--brown); line-height: 1;
    }
    .event-date .month { font-size: 0.65rem; letter-spacing: 0.16em; text-transform: uppercase; color: var(--blush); }
    .event-info h4 {
      font-family: 'Cormorant Garamond', serif; font-size: 1.15rem;
      font-weight: 600; color: var(--brown-dk); margin-bottom: 0.2rem;
    }
    .event-info p { font-size: 0.82rem; color: #9a7a6e; }
    .event-badge {
      font-size: 0.62rem; letter-spacing: 0.12em; text-transform: uppercase;
      padding: 0.2rem 0.6rem; border-radius: 2px; display: inline-block; margin-top: 0.4rem;
    }
    .badge-virtual { background: rgba(201,166,138,0.2); color: var(--brown); }
    .badge-inperson { background: rgba(107,79,66,0.1); color: var(--brown-dk); }
    .events-cta { margin-top: 2rem; text-align: center; }

    #contact {
      background: var(--brown-dk); color: var(--cream);
      text-align: center; padding: 7rem 4rem;
    }
    #contact .section-label { color: var(--blush); }
    #contact .section-title { color: var(--cream); margin: 0 auto 1rem; }
    #contact .section-body { color: var(--cream-dk); max-width: 500px; margin: 0 auto 3rem; opacity: 0.85; }
    .contact-form {
      max-width: 520px; margin: 0 auto;
      display: flex; flex-direction: column; gap: 1rem;
    }
    .form-row { display: grid; grid-template-columns: 1fr 1fr; gap: 1rem; }
    .contact-form input,
    .contact-form select,
    .contact-form textarea {
      width: 100%; padding: 0.85rem 1.2rem;
      background: rgba(245,237,229,0.07);
      border: 1px solid rgba(201,166,138,0.25);
      border-radius: 2px; color: var(--cream);
      font-family: 'Jost', sans-serif; font-size: 0.9rem;
      font-weight: 300; outline: none;
      transition: border-color 0.2s; -webkit-appearance: none;
    }
    .contact-form input::placeholder,
    .contact-form textarea::placeholder { color: rgba(245,237,229,0.4); }
    .contact-form select { color: rgba(245,237,229,0.7); cursor: pointer; }
    .contact-form select option { background: var(--brown-dk); color: var(--cream); }
    .contact-form input:focus,
    .contact-form select:focus,
    .contact-form textarea:focus { border-color: var(--blush); }
    .contact-form textarea { resize: vertical; min-height: 110px; }
    .contact-form button {
      padding: 1rem; background: var(--blush); border: none;
      border-radius: 2px; color: var(--brown-dk);
      font-family: 'Jost', sans-serif; font-size: 0.8rem;
      font-weight: 500; letter-spacing: 0.14em; text-transform: uppercase;
      cursor: pointer; transition: background 0.2s, transform 0.15s;
    }
    .contact-form button:hover { background: var(--blush-lt); transform: translateY(-1px); }

    footer {
      background: var(--brown-dk);
      border-top: 1px solid rgba(201,166,138,0.12);
      padding: 2rem 6rem;
      display: flex; justify-content: space-between; align-items: center;
    }
    .footer-logo { font-family: 'Cormorant Garamond', serif; font-size: 1.2rem; font-weight: 600; color: var(--blush); }
    footer p { font-size: 0.75rem; color: rgba(245,237,229,0.4); letter-spacing: 0.06em; }

    @keyframes fadeUp {
      from { opacity: 0; transform: translateY(24px); }
      to   { opacity: 1; transform: translateY(0); }
    }
    .reveal { opacity: 0; transform: translateY(20px); transition: opacity 0.7s ease, transform 0.7s ease; }
    .reveal.visible { opacity: 1; transform: translateY(0); }

    @media (max-width: 900px) {
      nav { padding: 1rem 1.5rem; }
      .nav-links { gap: 1.2rem; }
      #hero { grid-template-columns: 1fr; }
      .hero-visual { display: none; }
      .hero-content { padding: 8rem 2rem 5rem; }
      section { padding: 5rem 2rem; }
      #about, #events { grid-template-columns: 1fr; gap: 3rem; }
      .resources-header { grid-template-columns: 1fr; gap: 2rem; }
      .resources-grid { grid-template-columns: 1fr 1fr; }
      .about-pillars { grid-template-columns: 1fr; }
      .form-row { grid-template-columns: 1fr; }
      footer { flex-direction: column; gap: 1rem; text-align: center; padding: 2rem; }
    }
    @media (max-width: 600px) {
      .nav-links { display: none; }
      .resources-grid { grid-template-columns: 1fr; }
      .resource-card.featured { grid-row: span 1; }
    }
  </style>
</head>
<body>

  <nav>
    <a href="#hero" class="nav-logo">Our Terms</a>
    <ul class="nav-links">
      <li><a href="#about">About</a></li>
      <li><a href="#resources">Resources</a></li>
      <li><a href="#events">Events</a></li>
      <li><a href="#contact" class="nav-cta">Join Us</a></li>
    </ul>
  </nav>

  <section id="hero">
    <div class="hero-texture"></div>
    <div class="hero-content">
      <p class="hero-eyebrow">A community for Black professional women</p>
      <h1 class="hero-title">Our<br><em>Terms.</em></h1>
      <p class="hero-tagline">Our Work <span>•</span> Our Healing <span>•</span> Our Lives</p>
      <p class="hero-desc">
        Creating space, tools, and resources that empower Black women to navigate their work and career environments and define their pathway forward.
      </p>
      <div class="hero-actions">
        <a href="#contact" class="btn-primary">Join the Community</a>
        <a href="#about" class="btn-ghost">Our Mission</a>
      </div>
    </div>
    <div class="hero-visual">
      <div class="hero-svg-wrap">
        <svg viewBox="0 0 380 500" fill="none" xmlns="http://www.w3.org/2000/svg" style="width:100%;height:100%">
          <circle cx="240" cy="200" r="160" fill="rgba(201,166,138,0.07)" />
          <circle cx="240" cy="200" r="110" fill="rgba(201,166,138,0.05)" />
          <g opacity="0.65">
            <ellipse cx="130" cy="80" rx="30" ry="35" fill="rgba(201,166,138,0.25)" />
            <ellipse cx="130" cy="58" rx="38" ry="32" fill="rgba(201,166,138,0.18)" />
            <path d="M95 130 Q130 115 165 130 L175 300 Q130 318 85 300 Z" fill="rgba(201,166,138,0.2)" />
          </g>
          <g opacity="0.5">
            <ellipse cx="255" cy="90" rx="28" ry="32" fill="rgba(201,166,138,0.22)" />
            <circle cx="227" cy="105" r="5" fill="rgba(201,166,138,0.35)" />
            <circle cx="227" cy="116" r="3" fill="rgba(201,166,138,0.3)" />
            <ellipse cx="255" cy="72" rx="34" ry="26" fill="rgba(201,166,138,0.15)" />
            <path d="M272 92 Q282 132 277 185" stroke="rgba(201,166,138,0.3)" stroke-width="4" stroke-linecap="round" />
            <path d="M266 94 Q280 144 272 198" stroke="rgba(201,166,138,0.22)" stroke-width="3" stroke-linecap="round" />
            <path d="M220 142 Q255 127 288 142 L296 308 Q255 325 214 308 Z" fill="rgba(201,166,138,0.17)" />
          </g>
          <line x1="60" y1="390" x2="320" y2="390" stroke="rgba(201,166,138,0.15)" stroke-width="1" />
          <line x1="80" y1="404" x2="300" y2="404" stroke="rgba(201,166,138,0.1)" stroke-width="1" />
        </svg>
        <blockquote class="hero-quote">
          "In our world, divide and conquer must become define and empower."
          <cite>— Audre Lorde</cite>
        </blockquote>
      </div>
    </div>
  </section>

  <section id="about" class="reveal">
    <div>
      <p class="section-label">Who We Are</p>
      <h2 class="section-title">Built by Black women,<br><em>for Black women.</em></h2>
      <p class="section-body">
        Our Terms is a community-driven organization committed to equipping Black professional women with the knowledge, tools, and community they need to define and advance on their own terms. We meet women where they are and walk with them toward where they want to go.
      </p>
      <div class="about-pillars">
        <div class="pillar">
          <div class="pillar-icon">🌱</div>
          <h4>Growth Mindset</h4>
          <p>Addressing imposter syndrome and building lasting self-belief.</p>
        </div>
        <div class="pillar">
          <div class="pillar-icon">💼</div>
          <h4>Know Your Worth</h4>
          <p>Pay equity, salary research, and negotiation skills.</p>
        </div>
        <div class="pillar">
          <div class="pillar-icon">🤝</div>
          <h4>Leadership</h4>
          <p>From informal influence to formal leadership development.</p>
        </div>
        <div class="pillar">
          <div class="pillar-icon">🌸</div>
          <h4>Self-Care</h4>
          <p>Normalizing rest, boundaries, and grace as professional tools.</p>
        </div>
      </div>
    </div>
    <div class="about-visual">
      <div class="about-stat-card">
        <div class="stat-num">3&times;</div>
        <div class="stat-label">More likely to leave. Black women face unique retention barriers in the workplace.</div>
      </div>
      <div class="about-mission-box">
        <p class="section-label">Our Mission</p>
        <p>Creating space, tools, and resources so Black women can effectively navigate their learning and workplace environments and define their pathway forward.</p>
      </div>
      <div class="about-stat-card">
        <div class="stat-num">1 in 3</div>
        <div class="stat-label">Black women say they are unable to bring their authentic selves to work.</div>
      </div>
    </div>
  </section>

  <section id="resources" class="reveal">
    <div class="resources-header">
      <div>
        <p class="section-label">Our Toolkit</p>
        <h2 class="section-title">Resources built<br>for <em>your journey.</em></h2>
      </div>
      <p class="section-body">
        Our toolkit is structured like a university for professional Black women. Each pathway addresses a specific dimension of workplace navigation, from the inside out.
      </p>
    </div>
    <div class="resources-grid">
      <div class="resource-card featured">
        <div class="resource-num">01</div>
        <h3>Black Women's Workplace Toolkit</h3>
        <p>Our flagship curriculum. A structured guide covering mindset, policy, negotiation, leadership, and self-care. Designed for where you are now and where you are going.</p>
        <span class="resource-tag">Flagship Program</span>
      </div>
      <div class="resource-card">
        <div class="resource-num">02</div>
        <h3>Workplace Policies &amp; Authenticity</h3>
        <p>Understanding your rights, your employer's obligations, and how to show up as yourself without compromise.</p>
        <span class="resource-tag">Guides</span>
      </div>
      <div class="resource-card">
        <div class="resource-num">03</div>
        <h3>Salary &amp; Pay Equity</h3>
        <p>Research tools, scripts, and frameworks to understand, evaluate, and negotiate total compensation.</p>
        <span class="resource-tag">Tools</span>
      </div>
      <div class="resource-card">
        <div class="resource-num">04</div>
        <h3>Leadership Development</h3>
        <p>Networking, leadership styles, informal influence, and strategies for advancing with intention.</p>
        <span class="resource-tag">Courses</span>
      </div>
      <div class="resource-card">
        <div class="resource-num">05</div>
        <h3>Facilitated Sessions</h3>
        <p>Live conversations and workshops to work through challenges in real time, alongside community.</p>
        <span class="resource-tag">Community</span>
      </div>
    </div>
  </section>

  <section id="events" class="reveal">
    <div>
      <p class="section-label">What's Coming</p>
      <h2 class="section-title">Gather.<br><em>Learn. Grow.</em></h2>
      <p class="section-body">
        Our events are spaces designed for real conversation, skill-building, and connection. No performative allyship. Just community.
      </p>
    </div>
    <div>
      <div class="events-list">
        <div class="event-item">
          <div class="event-date"><div class="day">14</div><div class="month">Apr</div></div>
          <div class="event-info">
            <h4>Know Your Worth: Pay Equity Workshop</h4>
            <p>Research your salary, understand total rewards, and practice negotiation.</p>
            <span class="event-badge badge-virtual">Virtual</span>
          </div>
        </div>
        <div class="event-item">
          <div class="event-date"><div class="day">02</div><div class="month">May</div></div>
          <div class="event-info">
            <h4>Overcoming Imposter Syndrome</h4>
            <p>A facilitated conversation on self-doubt, identity, and professional confidence.</p>
            <span class="event-badge badge-virtual">Virtual</span>
          </div>
        </div>
        <div class="event-item">
          <div class="event-date"><div class="day">17</div><div class="month">May</div></div>
          <div class="event-info">
            <h4>Leadership Styles &amp; Informal Power</h4>
            <p>How to lead with intention before you have the title.</p>
            <span class="event-badge badge-inperson">In Person</span>
          </div>
        </div>
        <div class="event-item">
          <div class="event-date"><div class="day">07</div><div class="month">Jun</div></div>
          <div class="event-info">
            <h4>Self-Care as Strategy</h4>
            <p>Reframing rest, boundaries, and healing as professional tools, not luxuries.</p>
            <span class="event-badge badge-virtual">Virtual</span>
          </div>
        </div>
        <div class="event-item">
          <div class="event-date"><div class="day">21</div><div class="month">Jun</div></div>
          <div class="event-info">
            <h4>Community Mixer: Our Terms, Our Space</h4>
            <p>An informal gathering for connection, networking, and community building.</p>
            <span class="event-badge badge-inperson">In Person</span>
          </div>
        </div>
      </div>
      <div class="events-cta">
        <a href="#contact" class="btn-primary" style="display:inline-block;margin-top:1.5rem;">Get Event Updates</a>
      </div>
    </div>
  </section>

  <section id="contact" class="reveal">
    <p class="section-label">Join Us</p>
    <h2 class="section-title">Ready to navigate<br><em>on your own terms?</em></h2>
    <p class="section-body">
      Whether you are looking for community, resources, or both, we want to hear from you. Tell us where you are and we will meet you there.
    </p>
    <form class="contact-form" onsubmit="handleSubmit(event)">
      <div class="form-row">
        <input type="text" placeholder="First Name" required />
        <input type="text" placeholder="Last Name" required />
      </div>
      <input type="email" placeholder="Email Address" required />
      <input type="text" placeholder="Industry / Field" />
      <select>
        <option value="" disabled selected>What brought you here?</option>
        <option>Community &amp; Connection</option>
        <option>Salary &amp; Pay Equity</option>
        <option>Leadership Development</option>
        <option>Workplace Navigation</option>
        <option>Self-Care &amp; Wellness</option>
        <option>Events &amp; Workshops</option>
        <option>All of the above</option>
      </select>
      <textarea placeholder="Anything else you'd like us to know (optional)"></textarea>
      <button type="submit">Submit &rarr;</button>
    </form>
  </section>

  <footer>
    <div class="footer-logo">Our Terms</div>
    <p>Our Work &bull; Our Healing &bull; Our Lives</p>
    <p>&copy; 2026 Our Terms. All rights reserved.</p>
  </footer>

  <script>
    const reveals = document.querySelectorAll('.reveal');
    const observer = new IntersectionObserver((entries) => {
      entries.forEach(e => {
        if (e.isIntersecting) { e.target.classList.add('visible'); observer.unobserve(e.target); }
      });
    }, { threshold: 0.08 });
    reveals.forEach(el => observer.observe(el));

    function handleSubmit(e) {
      e.preventDefault();
      const btn = e.target.querySelector('button');
      btn.textContent = 'Thank you! We will be in touch.';
      btn.style.background = 'var(--brown)';
      btn.style.color = 'var(--cream)';
      btn.disabled = true;
      e.target.reset();
    }
  </script>
</body>
</html>
